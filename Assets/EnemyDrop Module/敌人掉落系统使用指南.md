# 敌人掉落系统使用指南

## 📋 概述

本项目包含两套掉落系统，分别位于不同的模块：

1. **EnemyDrop 模块**：完整的掉落系统，包含物品管理、自动拾取、事件系统等
2. **Enemy 模块**：简化版的掉落系统，仅负责掉落生成

本指南主要介绍如何使用 **EnemyDrop 模块** 的完整掉落系统为敌人添加掉落功能。

---

## 🎯 快速开始

### 第一步：场景准备

确保场景中有 `DropManager` 管理器：

1. 在场景中创建一个空的 GameObject，命名为 `DropManager`
2. 为该 GameObject 添加 `DropManager` 组件
3. 在 Inspector 中配置参数（可选）：
   - `Enable Drops`：是否启用掉落系统
   - `Auto Pickup Range`：自动拾取范围（默认 1.5）
   - `Item Lifetime`：物品存在时间（默认 30 秒）

### 第二步：创建掉落表（DropTable）

1. 在 Project 窗口中右键点击
2. 选择 `Create > Roguelike > Drop Table`
3. 命名掉落表（例如：`BasicEnemyDropTable`）
4. 在 Inspector 中配置掉落表：

```
基本信息
├── Enemy Name: 敌人名称（例如："基础敌人"）
├── Min Total Drops: 最小掉落数量（例如：1）
└── Max Total Drops: 最大掉落数量（例如：3）

可能掉落物品列表
├── 点击 "+" 添加物品
├── 为每个物品配置：
│   ├── Item Name: 物品名称
│   ├── Item Prefab: 物品预制体
│   ├── Item Icon: 物品图标（可选）
│   ├── Drop Chance: 掉落概率（0-1）
│   ├── Min Quantity: 最小数量
│   ├── Max Quantity: 最大数量
│   ├── Item Type: 物品类型（Health/Mana/Coin）
│   └── Apply Force: 是否掉落时施加力（物理效果）
│
└── 金币设置
    ├── Always Drop Coin: 是否总是掉落金币
    ├── Min Coins: 最小金币数
    ├── Max Coins: 最大金币数
    └── Coin Drop Chance: 金币掉落概率
```

### 第三步：为敌人添加掉落组件

为敌人 GameObject 添加 `EnemyDeathDetector` 组件：

1. 选择敌人 GameObject
2. 在 Inspector 中点击 `Add Component`
3. 搜索并添加 `EnemyDeathDetector` 组件
4. 配置参数：

```
设置
├── Drop Table: 拖入刚才创建的 DropTable 资源
├── Enable Drops: 是否启用掉落
└── Drop Offset: 掉落位置偏移（默认：(0, 0.5, 0)）

健康检测设置
├── Health Check Interval: 健康检测间隔（默认：0.5 秒）
└── Health Component Name: 健康组件名称（默认："Attribute"）
```

### 第四步：准备掉落物品预制体

确保掉落物品预制体包含以下组件：

1. **DropItemController**：必需组件
   - 自动添加浮空效果
   - 管理拾取逻辑

2. **Rigidbody2D**：可选（如果需要物理效果）
   - 启用 `Gravity Scale` 为 0（如果不想受重力影响）

3. **Collider2D**：必需
   - 设置为 `Trigger`
   - 用于检测玩家碰撞

4. **SpriteRenderer**：必需
   - 显示物品图标

**预制体示例**（参考 `Assets/EnemyDrop Module` 下的预制体）：
- `Coin.prefab`：金币
- `HealthPotion.prefab`：生命药水
- `ManaPotion.prefab`：魔法药水

---

## 🔧 系统工作原理

### 掉落流程

1. 敌人死亡检测：`EnemyDeathDetector` 每 0.5 秒检查一次 `Attribute.CurrentHealth`
2. 当血量 ≤ 0 且上一帧血量 > 0 时，触发掉落
3. 从掉落表 `DropTable` 中随机选择物品
4. `DropManager` 实例化物品并施加物理力
5. 物品自动拾取：玩家靠近时自动拾取

### 组件职责

| 组件 | 职责 |
|------|------|
| **DropTable** | 掉落表配置，定义可能掉落的物品和概率 |
| **DropItem** | 物品数据结构，包含掉落概率、数量等信息 |
| **DropManager** | 全局管理器，负责掉落生成、自动拾取、事件触发 |
| **EnemyDeathDetector** | 敌人死亡检测，触发掉落 |
| **DropItemController** | 物品行为控制，浮空、拾取、销毁 |
| **DropEventListener** | 事件监听器，可监听掉落相关事件 |

### 事件系统

`DropManager` 提供三个事件，可用于连接其他系统：

```csharp
// 金币收集事件
public System.Action<int> OnCoinCollected;

// 生命恢复事件
public System.Action<int> OnHealthRestored;

// 魔法恢复事件
public System.Action<int> OnManaRestored;
```

使用示例（参考 `DropEventListener.cs`）：

```csharp
void Start()
{
    if (DropManager.Instance != null)
    {
        DropManager.Instance.OnCoinCollected += OnCoinCollected;
        DropManager.Instance.OnHealthRestored += OnHealthRestored;
        DropManager.Instance.OnManaRestored += OnManaRestored;
    }
}

void OnDestroy()
{
    if (DropManager.Instance != null)
    {
        DropManager.Instance.OnCoinCollected -= OnCoinCollected;
        DropManager.Instance.OnHealthRestored -= OnHealthRestored;
        DropManager.Instance.OnManaRestored -= OnManaRestored;
    }
}
```

---

## 🛠️ 调试功能

### 快捷键测试

在场景中添加 `DropSystemTester` 组件，可以使用以下快捷键：

| 按键 | 功能 |
|------|------|
| **T** | 测试掉落（触发一次掉落） |
| **C** | 清除所有掉落物品 |
| **I** | 显示掉落信息 |
| **G** | 显示当前金币总数 |

### 右键菜单

在 Inspector 中右键点击组件，可使用上下文菜单：

**EnemyDeathDetector**：
- `测试掉落`：立即触发一次掉落
- `显示掉落信息`：在控制台输出掉落配置信息

**DropManager**：
- `测试掉落系统`：显示系统状态和活动物品数量

---

## 📊 掉落表配置示例

### 基础敌人掉落表

参考 `BasicEnemyDropTable.asset`：

```
敌人名称：基础敌人
掉落数量：1-2 个物品
掉落列表：
├── 生命药水
│   ├── 掉落概率：40%
│   ├── 数量：1-2
│   └── 类型：Health
│
├── 魔法药水
│   ├── 掉落概率：35%
│   ├── 数量：1-2
│   └── 类型：Mana
│
└── 金币
    ├── 掉落概率：80% (alwaysDropCoin)
    ├── 数量：1-3
    └── 类型：Coin
```

---

## 🔗 与 Attribute 系统集成

### Attribute 组件

`EnemyDeathDetector` 通过反射查找 `Attribute` 组件的健康属性：

```csharp
// 查找组件
MonoBehaviour[] components = GetComponents<MonoBehaviour>();
foreach (var component in components)
{
    if (component.GetType().Name == healthComponentName)
    {
        healthComponent = component;
        break;
    }
}

// 通过反射获取属性
currentHealthProperty = type.GetProperty("CurrentHealth");
maxHealthProperty = type.GetProperty("MaxHealth");
```

### 健康检测

每 0.5 秒检测一次：

```csharp
if (currentHealth <= 0 && lastHealth > 0)
{
    OnEnemyDeath();  // 触发掉落
    isDead = true;
}
```

---

## ⚠️ 常见问题

### Q1: 敌人死亡后不掉落物品

**检查清单**：
- ✅ 场景中是否有 `DropManager` GameObject？
- ✅ `DropManager` 组件是否正确添加？
- ✅ 敌人是否有 `EnemyDeathDetector` 组件？
- ✅ `Drop Table` 是否已分配？
- ✅ `Enable Drops` 是否勾选？

### Q2: 物品不掉落物理效果

**解决方案**：
- 检查物品预制体是否有 `Rigidbody2D` 组件
- 在 `DropItem` 配置中确保 `Apply Force` 为 `true`
- 调整 `Force Multiplier` 的值

### Q3: 物品不被自动拾取

**检查清单**：
- ✅ 玩家 GameObject 是否有 "Player" 标签？
- ✅ 物品的 `Collider2D` 是否设置为 `Trigger`？
- ✅ `DropManager` 的 `Auto Pickup Range` 是否设置合理？
- ✅ 物品生成后是否等待了 `Pickup Delay` 时间（默认 0.5 秒）？

### Q4: 掉落物品突然消失

**原因**：
- 物品存在时间到了（默认 30 秒）
- 被 `ClearAllDrops()` 清除

**解决方案**：
- 增加 `DropManager` 的 `Item Lifetime` 值
- 或者移除定时销毁逻辑

---

## 🎮 使用场景示例

### 示例 1：为 BOSS 添加稀有掉落

```
1. 创建新的 DropTable（例如：BossDropTable.asset）
2. 设置掉落数量：2-4 个
3. 添加稀有物品：
   - 传说武器：掉落概率 10%
   - 史诗盔甲：掉落概率 15%
   - 大量金币：掉落概率 100%
4. 将 DropTable 分配给 BOSS 的 EnemyDeathDetector
```

### 示例 2：创建多级掉落

```
1. 创建多个 DropTable：
   - NormalEnemyDropTable（普通敌人）
   - EliteEnemyDropTable（精英敌人）
   - BossDropTable（BOSS）
2. 使用 EnemyDeathDetector 的 `Drop Table` 字段切换不同掉落
3. 或者根据敌人等级动态切换
```

---

## 📝 扩展建议

### 添加新的掉落物品类型

修改 `DropItemType` 枚举：

```csharp
public enum DropItemType
{
    Health,      // 生命恢复
    Mana,        // 魔法恢复
    Coin,        // 金币
    Weapon,      // 武器（新增）
    Armor,       // 盔甲（新增）
    Consumable   // 消耗品（新增）
}
```

在 `DropItemController.ApplyItemEffect()` 中添加处理逻辑：

```csharp
switch (itemType)
{
    case DropItemType.Health:
        DropManager.Instance.TriggerHealthRestored(amount);
        break;
    
    case DropItemType.Weapon:
        // 处理武器掉落逻辑
        break;
    
    // ...
}
```

### 添加掉落动画

在 `DropItemController` 中扩展视觉效果：

```csharp
// 添加生成时的缩放动画
void Start()
{
    StartCoroutine(ScaleInAnimation());
}

IEnumerator ScaleInAnimation()
{
    transform.localScale = Vector3.zero;
    while (transform.localScale.x < 1f)
    {
        transform.localScale += Vector3.one * Time.deltaTime * 2f;
        yield return null;
    }
}
```

---

## 📚 相关文件

### EnemyDrop 模块

| 文件 | 说明 |
|------|------|
| `DropTable.cs` | 掉落表脚本 |
| `DropItem.cs` | 掉落物品数据结构 |
| `DropManager.cs` | 掉落管理器 |
| `EnemyDeathDetector.cs` | 敌人死亡检测器 |
| `DropItemController.cs` | 掉落物品控制器 |
| `DropEventListener.cs` | 掉落事件监听器 |
| `DropSystemTester.cs` | 掉落系统测试器 |
| `BasicEnemyDropTable.asset` | 基础掉落表示例 |

### 预制体

| 预制体 | 说明 |
|--------|------|
| `Coin.prefab` | 金币掉落物 |
| `HealthPotion.prefab` | 生命药水掉落物 |
| `ManaPotion.prefab` | 魔法药水掉落物 |
| `Enemy.prefab` | 带掉落功能的敌人示例 |

---

## 🎯 总结

使用掉落系统的核心步骤：

1. ✅ 在场景中添加 `DropManager`
2. ✅ 创建 `DropTable` 资源配置
3. ✅ 为敌人添加 `EnemyDeathDetector` 组件
4. ✅ 准备掉落物品预制体
5. ✅ 测试和调试

遵循这些步骤，您可以轻松为任何敌人添加丰富的掉落功能！

---

**文档版本**：1.0  
**最后更新**：2024年

